version: '3.8'

services:
  backend:
    image: backend:latest

    privileged: true

    network_mode: host


    volumes:
      - /etc:/host-etc:ro

    environment:
      - DATABASE_PASSWORD=secret123
      - API_SECRET_KEY=supersecret

    ports:
      - "8000:8000"
      - "5432:5432"
